#############################################
# Environment Variables
# Copy this file to `.env` or `.env.local` (Vite loads both) and fill values.
#############################################

# Formspree endpoint:
# 1. Visit https://formspree.io/ and create a new form.
# 2. Copy the provided endpoint that looks like: https://formspree.io/f/abcdwxyz
# 3. Paste it below. Leave blank to disable the contact form (UI will show a notice).
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/mgvnojzo

# Optional: generate bundle stats (run once, don't commit stats html if large)
# BUNDLE_ANALYZE=1

# Optional: Google reCAPTCHA v3 site key (public). If set, form will request a token.
# Get from: https://www.google.com/recaptcha/admin/create (choose reCAPTCHA v3)
# VITE_RECAPTCHA_SITE_KEY=
